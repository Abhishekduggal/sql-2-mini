Answers for the Mini

/* CREATE TABLE movie (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title VARCHAR (80),
  media_type_id REFERENCES MediaType(MediaTypeId)
  )
/* /*    */
   
/*   INSERT INTO Movie(Title, media_type_Id) VALUES ('HarryPotter', 2); */ 

/* SELECT * FROM movie; */

/* ALTER TABLE movie ADD COLUMN Genreid INTEGER REFERENCES Genre(GenreId); */

/* SELECT * FROM movie; */

/* UPDATE movie SET GenreId = 22 where id = 1; */

/* SELECT * FROM movie; */

/* SELECT * FROM Artist 
JOIN Album ON Album.title = Artist.name;
 */

/* SELECT Album.title, Artist.name 
FROM Artist 
JOIN Album ON Album.ArtistId = Artist.ArtistId */



/* SELECT Album.title, Artist.name 
FROM Artist 
JOIN Album ON Album.ArtistId = Artist.ArtistId */

/* SELECT * FROM TRACK 
WHERE GenreID IN (SELECT GenreId FROM Genre WHERE Name = 'Jazz' OR Name = 'Blues');
 */
 
 
/*  UPDATE Employee SET Phone = null WHERE EmployeeId = 1; */

/* SELECT * FROM Employee WHERE EmployeeId = 1; */

/* SELECT * FROM Customer WHERE Company IS Null; */

/* SELECT Artist.ArtistId, Artist.Name, count(*)
FROM Artist 
JOIN Album ON Artist.ArtistId = Album.ArtistId
GROUP BY Artist.ArtistId; */
/* 
SELECT Artist.ArtistId, Artist.Name, count(*)
FROM Artist 
JOIN Album ON Artist.ArtistId = Album.ArtistId
GROUP BY Artist.ArtistId;
ORDER BY Artist.Album DESC; */

/* SELECT DISTINCT (Country) FROM Customer; */

/* SELECT * FROM Customer WHERE Fax IS Null; */

/* DELETE FROM Customer WHERE Fax IS Null; */
